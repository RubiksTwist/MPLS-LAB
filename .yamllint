---
extends: default

rules:
  line-length:
    max: 120
    level: warning
  truthy:
    check-keys: false  # Allows yes/no in keys which is common in Ansible
  comments:
    min-spaces-from-content: 1  # More flexible comment spacing

ignore: |
  .venv/
  ansible/backups/
  .github/